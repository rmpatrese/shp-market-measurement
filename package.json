{
  "name": "solar-fakedoor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "emulators": "firebase emulators:start --only firestore,hosting,auth"
  },
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
